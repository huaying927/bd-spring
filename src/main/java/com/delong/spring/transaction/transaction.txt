事务:spring 事务一般加在业务层(service)的方法上
1.事务的使用场景：
  (1)秒杀系统
  (2)金融系统
2.事务的特征：ACID
  (1)原子性
  (2)一致性
  (3)隔离性：isolation
     事务的隔离级别：
     <1>未提交读(READ UNCOMMITTED)
     <2>已提交读(READ COMMITTED)
     <3>可重复度(REPEATABLE READ)
     <4>序列化(SERIALIZABLE)
     备注: 从安全性的角度说，自上而下安全性越来越高；
          从性能的角度说，自上而下性能越来越低。
  (4)持久性
3.事务的并发问题
  (1)脏读：事务A读取了事务B更新的数据，然后B回滚操作，那么A读取到的数据是脏数据
  (2)不可重复读：事务 A 多次读取同一数据，事务 B 在事务A多次读取的过程中，对数
     据作了更新并提交，导致事务A多次读取同一数据时，结果不一致
  (3)幻读：系统管理员A将数据库中所有学生的成绩从具体分数改为ABCDE等级，但是系统
     管理员B就在这个时候插入了一条具体分数的记录，当系统管理员A改结束后发现还有一
     条记录没有改过来，就好像发生了幻觉一样，这就叫幻读
  小结：不可重复读的和幻读很容易混淆，不可重复读侧重于修改，幻读侧重于新增或删除。
       解决不可重复读的问题只需锁住满足条件的行，解决幻读需要锁表
4.数据库的事务(MySQL)：是否支持事务是依赖于MySQL数据库的引擎，
  MySQL5.5版本之前的默认引擎是MyISAM, MySQL5.5版本之后的默
  认引擎是InnoDB，该引擎支持事务、行级锁以及主外键。
5.MySQL数据库常用引擎及特点(扩展)
  show engines\G;
6.存储过程和函数的区别(扩展)
7.index
8.join和UNION
9.spring 事务的实现方式：
  (1)编程式事务:
  (2)声明式事务(重点)
      <1>EnableTransactionManager
      <2>Transactional
10.web容器、web服务器和应用服务器的区别
  (1)web容器是抽象的概念 符合java web规范的容器就是web容器
  (2)web服务器是web容器的具体实现，Tomcat Jetty
  (3)应用服务器是实现Java EE所有规范 JBoss WebLogic WebSphere



